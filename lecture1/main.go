package main 

import(
	"fmt"
)

//every program must have a func main(), it is where the program starts
//when we declare a function, we put 'func' keyword infront of it
func main(){

	//we are using Println from fmt package, so we prefix it with the fmt package
	fmt.Println("Hello, Praveen!")
}